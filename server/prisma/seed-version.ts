// Единственный источник правды для версии сида.
// Значение читают и сам сид (записывает его в Setting.seedVersion), и гейт
// scripts/seed-if-needed.ts (сравнивает с записанным в базе). Раньше константа
// была продублирована в двух файлах и разъехалась — сид молча не запускался.
// Поднимайте версию, когда база должна быть пересоздана при следующем деплое.
export const SEED_VERSION = '4';
